# Plan: Task 028 Phase A — 1M particles in 10 ms feasibility (measurement + report)

Self-contained: a fresh agent should read this file, then the subitem files in
`plans/20260731_028/` in order, and execute. **No repo exploration needed** — all
numbers, signatures, and paths in the subitems were verified 2026-07-31.

## Objective

Task `MATRIX_OPERATOR_REFACTOR/028-performance-feasibility-1m-in-10ms.md` (read it
first; task-local source of truth): can n=1e6 bodies run in ≤0.01 s **per time step**
on one H200 at `expansion_order=3` (P=4), resident CUDA lifecycle? This plan covers
**Phase A only**: fill measurement gaps, classify per-stage bounds, deliver a
prioritized optimization list. The task file mandates a **user-approval pause before
Phase B** — end the session at that checkpoint; do not start optimizing.

Verdict boundary = refresh (tree/routes) + full resident evaluation + device Euler
convection, no per-step body H2D/D2H. Also report: evaluation-only and
including-transfers boundaries. Float32 qualifies if sampled error ≤ P=4 Float64
truncation error (already ~established: 1.1889e-4 vs 1.1873e-4 at the flat n=1e6
point; re-verify on the hierarchical path).

## Headline facts (details + sources in `00-context-evidence.md`)

- Only measured n=1e6 point: **flat** dense ell=4, 0.4246 s F64 / 0.3213 s F32 —
  42x/32x over target.
- Hierarchical path (now default): **no n=1e6, no Float32, no LH data at all** —
  these are the gaps Phase A fills.
- At n=2e5 hierarchical, best step 29.98 ms (hier3) / 48.56 ms (hier12,
  accuracy-matched); step is bound by **host allocation/GC (~45 ms of 60.8 at
  ell=5)** and the **fused L2B+nearfield stage**, not M2L.
- hier3's gradient error (~6e-4) is below the P=4 accuracy gate; **hier12 is the
  presumptive verdict configuration**.
- No device convection code exists — the harness must add it (test/benchmark side
  only; **no production `src/` changes in Phase A**).

## Subitems (execute in order; record progress in each file's Progress section)

| File | Content |
|---|---|
| `20260731_028/00-context-evidence.md` | Consolidated benchmark record, open residuals, accuracy numbers, established bound classifications |
| `20260731_028/01-code-surface.md` | Verified API recipe (construction, per-step calls, stage timing, counters, outputs) + cluster quirks |
| `20260731_028/02-phase-harness.md` | Build `benchmark_028_feasibility.jl`, device-resident system + Euler kernel, `cuda_028_{submit,run,fetch}.sh` |
| `20260731_028/03-phase-pilot.md` | Pilot H200 job: validate capacity, convection, counters at n=1e6 |
| `20260731_028/04-phase-sweep.md` | Full measurement campaign matrix (heavy cluster run) |
| `20260731_028/05-phase-report.md` | Bound classification, `data/feasibility_1m_10ms/report.md`, lever list, user checkpoint |

## Verification (summary; details in phase files)

Counter contract asserted per case; sampled-direct error hard-gate (≤10x of 1.19e-4
per 024b precedent) with F32 admissibility recorded; convection sanity re-check after
k steps; fill 028 task-file Implementation/Verification Notes (leave Done/Approved
unchecked — Phase B remains).
