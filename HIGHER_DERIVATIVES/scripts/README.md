# scripts/

Verification, prototype, and benchmark scripts, named after their owning item
(e.g. `T1_verify_recurrence.jl`). Run via the `julia-test-runner` agent, output redirected to a
log; never paste raw output into context. Start at `../START_HERE.md`.
