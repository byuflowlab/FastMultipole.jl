# 052c — stage-d performance overhaul (plan, 2026-08-26)

Status: PLANNED (high-level plan RYAN-APPROVED 2026-08-26). 052 and
052a are CLOSED (Ryan, 2026-08-26): this item now OWNS the 1080-step
stage-d acceptance (inherited from 052 Phase D) plus the 052/052a
consolidation + notebook entry. b200 support was dropped (13477878
cancelled before start) → the frozen-tree constraint is LIFTED. Next
agent: read "Context" first (incl. the Review addendum) — it contains
everything needed, do not re-derive. P0-sync/P1/P1.5 are DONE (see their
status sections). **Current state (2026-08-26 night): timer probe 13494373 HARVESTED —
read "Probe RESULTS" below. Verdict: with the radix cache built at
mature N (warm restart), the existing pipeline runs the mature window
at ~5.8 s/step — already under the 10 s target. The frozen-ell=2 cache
was the whole wake-side pathology; P2's heavy restructure is NOT
needed. Deploy-critical change = the FLOWVPM depth-rebuild fix
(uncommitted).** Review items: I2/I3/I4/I5 APPROVED (I3 with a revisit
caveat under P2); I6 resolved (P3 SKIPPED); I1-monitors ruled leave
untouched. **All post-probe rulings are in — see "Rulings + execution
(2026-08-26, post-probe session)": acceptance path approved, retirees
cleaned up, h200 deploy done, mature-gate wrapper staged awaiting
Ryan's go.**

**Goal:** the 052 stage-d acceptance (exact 1080-step rotor-hover run,
all gates) completes well inside a 4 h H200 allocation. Target plateau
step cost ≤10 s/step at N≈230k (measured 2026-08-26: ~70 s/step and
still climbing at step 407; job 13484013 timed out at 4 h at step
473/1079, final-block mean ~66 s/step).

## Context — evidence, all verified 2026-08-26

Phase-1 telemetry mining (398 stage-d steps of h200 chain 13484013 +
wake-health N per step; parser + per-step CSV + raw log preserved in
`MATRIX_OPERATOR_REFACTOR/data/052c_phase1/` — parse with a script,
never read the CSVs directly):

| category | s/step @ steps 1–67 | @ 331–398 | scaling fit |
|---|---|---|---|
| total step | 4.6 | 51.5 | (N^0.77, diluted) |
| wake_influence | 1.3 | 44.5 (86%) | N^1.47 |
| — nested wake_sfs | 1.1 | 29.5 | N^1.48 |
| — gpu influence_pass1 (2 calls/step) | 0.2 | 14.8 | N^1.58 |
| body_influence (= gpu pass3) | 1.1 | 2.8 | N^0.27 |
| io + monitors | 0.7 | 3.5 | ~N^0.5 |
| solve | 0.7 | 0.7 | flat |

Timer coverage is complete (total_step − sum(categories) = 0.00 s).
N reaches 230k by step 399 (above the 209k mature-window level — count
peaks mid-run). At matched N the GPU step (~51 s) ≈ CPU reference
(52–56 s at 209k): GPU-S buys nothing on the dominant terms at scale.

Source grounding (file:line verified by exploration agent 2026-08-26):

- `gpu_timer influence_pass1/pass3` = `FastMultipole.direct_rectangular!`
  — DENSE brute-force O(N_t×N_s), NOT FMM (seam:
  `FLOWPanel.jl/src/FLOWPanel_gpu_influence.jl:648,652-657`; kernel
  `FastMultipole/src/direct_rectangular.jl:762-790`). pass1 =
  wake→bodies/probes/particles: the particle-target portion makes both
  sides grow with the wake → genuinely quadratic (the N^1.58). pass3 =
  bodies→everything: fixed body count → flat.
- `wake_sfs` = `FLOWVPM.Estr_fmm!` walking the FMM near-field
  `direct_list` pairwise ON CPU host threads
  (`FLOWPanel.jl/src/FLOWPanel_wake.jl:2098-2103`,
  `FLOWVPM.jl/src/FLOWVPM_subfilterscale_models.jl:115-218`), on the
  tree built with the pinned p=4/ncrit=50/theta=0.4 triple
  (`FLOWPanel.jl/src/FLOWPanel_fmm.jl:111-119`, driver pin at
  `rotor_hover_pressure_comparison.jl:670-679`). N^1.48 = near-field
  pair densification: thin helical wake + fixed leaf capacity/theta
  never tuned for plateau N.
- Prior-item facts (refactor-docs librarian, 2026-08-26): fastest
  measured GPU config = device-resident RadixFMMCache with
  DenseTranslationM2L (fastest at all 14 (n,precision) points, 024b:223;
  94–103× vs 1-thread CPU at n=1e6 on H200), fused nearfield wins
  ≥400k bodies (041e:317-357); 048 delivered SFS/Estr accumulation
  on-device via SFS-armed RadixFMMCache but it REQUIRES a fully
  CuArray-backed (device-resident) ParticleField — host-backed pfield
  is exactly why wake_sfs runs on CPU; radix FMM v1 restriction:
  `targets === sources` (050:34-39) — cross-system FMM designed
  (052b Part 1) but NOT implemented; do not need it (see P2).

**Review addendum (2026-08-26, verified):** an independent plan review
(`052c-review-improvements-2026-08-26.local.md` — read it; items I2–I6
there are PROPOSED, not yet approved) refit all 473 harvested steps
(script: `data/052c_phase1/052c_review_fits.py`) and found:

- Steeper exponents than the table above (473 vs 398 steps): wake_sfs
  N^1.59, g_influence_pass1 N^1.69, wake_influence N^1.57.
- N was **still climbing** at the last measured step (242,348 at step
  454, ~126/step); the CPU mature window (steps 720–755) sits at ~209k,
  so the true peak is >242k, somewhere in steps 455–720, and was never
  observed. Budget/tuning should target peak N, not 230k.
- Budget floor: the categories P2 does not restructure sum to ~6.8
  s/step at 230k — io 2.1 (N^0.52) + monitors 1.1 (N^0.72) + pass3 2.6
  (N^0.30) + solve 0.8 + misc 0.2 — leaving only ~3.2 s of the 10 s
  target for the GPU FMM before the P1.5 io fix below (after it: ~5 s+).
- io root cause (measured locally, exact write shape @230k): the
  per-step full-particle .vtp (`FLOWPanel_wake.jl:2264-2282`) costs
  1.8–1.9 s with `compress=true` (default) vs 0.17–0.24 s uncompressed
  (52.4 vs 57.0 MB — zlib buys ~8% on float64 particle data). Both
  modes are binary (`format="appended"`).

**Decisions already made (Ryan 2026-08-26):**
- **io fix APPROVED (see P1.5):** `compress=false` + Float32 output
  arrays at the .vtp write site. **REJECTED:** a save-interval knob —
  the VTK series feeds visuals needing full temporal resolution.
  **DEFERRED:** eliminating the ~4 redundant D2H mirror syncs/step
  (wake-health/attribution/inventory/VTK each re-sync ~67 MB, ~60–100
  ms/step total; candidate fix = memoize on `pfield.nt`) — revisit
  later if profiling cares.
- Do NOT extend broken/slow runs; fix the scaling.
- Placement: put changes where they make the most sense (no
  FastMultipole-only constraint). FLOWVPM/FLOWPanel edits change the
  science fingerprint + arithmetic → regenerate the pinned CPU reference
  (deterministic now) and re-run affected gates. FastMultipole edits are
  outside the fingerprint (evidence: h100 passed without 8479d0f2, h200
  passed with it).
- 052b's "device-resident Phase B blocked on 052 acceptance" gate is
  DELIBERATELY INVERTED here: acceptance is blocked on this slowness.
- Panels-on-self influence: cache a priori (rigid assembly ⇒ constant in
  body frame) and reuse every step.

## Plan

### P0 — consolidation (small; inherited from 052/052a closure)
With b200 dropped, no frozen trees remain. Sync FastMultipole 8479d0f2
(recenter!-sfs fix) to the arch FM trees + FLOWVPM-052-*; decide with
Ryan which cluster checkouts 052c will actually use going forward (one
tree is simpler now that per-arch validation is over) and retire the
rest. Offer (do not write) the 052/052a closure notebook entry.

### P1 — wiring audit (small)
Map interaction × algorithm as ACTUALLY executed in stage d: particle
self-UJ, Estr/SFS, wake→panels, wake→probes, panels→particles,
panels→self. Specifically: what each of the 2 `influence_pass1` calls
per step covers (probe_pass flag at FLOWPanel_gpu_influence.jl:652), and
how the RadixFMMCache (target of the 8479d0f2 recenter!-sfs fix) coexists
with a host pfield — i.e. which path computes particle-particle UJ today.
Deliverable: a table in this doc. Entry points: `_sa_wake_influence!`
(FLOWPanel_simulate.jl:708-712), `_gpu_rect_influence!`
(FLOWPanel_gpu_influence.jl), FLOWPanel_gpu_wake.jl:156-168.

### P1.5 — io fix (small; APPROVED Ryan 2026-08-26)
At the particle .vtp write site (`FLOWPanel_wake.jl:2264-2282`):
pass `compress=false` and hand WriteVTK Float32 arrays (convert the
views, e.g. `Float32.(view(...))`; WriteVTK then emits
`type="Float32"`). Expected io category: ~2.0 → ~0.1 s/step at plateau;
~28 MB/step on disk. Output-only → outside the science fingerprint; no
reference regeneration needed for this alone. NOTE: if P2 ends up
making the pfield Float32 end-to-end, the host mirror is already
Float32 and the conversion cost disappears entirely (keep that in mind
when choosing P2 precision — precision IS inside the fingerprint; 048
measured F32 e_delivered ~1e-3 vs F64 ~5e-4). Monitors decimation was
NOT approved — leave monitors untouched pending review item I1's
monitor half.

### P2 — all-FMM routing + device residency (heavy; the main work)
> **I3 APPROVED (Ryan 2026-08-26), with a caveat:** reuse the existing
> split-call pattern in `_sa_wake_influence!` (see the review doc's I3)
> as the P2 routing mechanism — but **revisit the approach if the split
> wiring is still expensive** after measurement (i.e. don't defend the
> pattern if the probe timers say the cost is in the call structure
> itself). Other review items I2/I4/I5/I6 remain unruled.
- Particle self-interaction (UJ) + SFS through the device-resident,
  SFS-armed radix GPU FMM (DenseTranslationM2L; fused nearfield if
  N ≥ ~400k). `targets===sources` holds for this — no 052b Part 1
  cross-system lift needed.
- Keep `direct_rectangular!` ONLY for fixed-small target sets
  (wake→panel surface points, wake→probes: linear in N) and
  panels→particles (fixed source count: linear in N).
- Panels-on-self: precompute AIC once, reuse (ceiling ~3.5 s/step from
  body_influence+solve, but cheap and permanent).
- Requires CuArray-backed ParticleField in the driver loop (052b Phase
  A.1 shared-pfield infrastructure is complete. Its formerly blocking
  warm-start NaN was root-caused and FIXED 2026-08-26 — missing
  freestream/velocity_te staging before `simulate_warmstart!`'s
  end-of-step replay; `runtests_unit_warmstart.jl` now 153/153 — see
  "Blocker RESOLVED" in `052b-impl-multirotor-ige-gpu.md`. Fix is
  uncommitted in the FLOWPanel worktree.) Watch
  recenter! frequency at scale (in-place recenter! design + measured
  costs in FUTURE_IMPROVEMENTS.local.md — pull it in if profiling says
  so).

### P3 — retune the fixed deterministic FMM triple at plateau N (moderate)
Local (no GPU, ≤4 threads): load a late-step particle snapshot from the
stage-d run dir (`orc:FLOWPanel-052/data/fm052d_gpu_1080/`, ~230k
particles), build trees over a (leaf_size, theta[, p]) sweep, count
`direct_list` pairs → predicted near-field cost → candidate fixed
triples. One GPU job validates timing. The chosen triple stays FIXED
(determinism); applies to whatever trees remain (device radix + CPU
reference).

### P4 — gates and acceptance (moderate)
- Every change validated with the 36-step mature gate (~10–25 min,
  count_tol=16 vs pinned reference) BEFORE any 1080-step attempt.
- End state: regenerate the pinned CPU reference (arithmetic changes:
  dense→FMM), re-run affected gates, then the full stage-d acceptance —
  FIRST attempt at 12 h limit (qos=gpu MaxWall is 3 days), right-size
  after measuring.

## P1 — wiring audit RESULTS (2026-08-26, session after the review)

Evidence tiers: **[V]** verified against the stage-d log of job 13484013
(`data/052c_phase1/fp052chain-13484013.out.gz`, stage-d section) or by
direct source read; **[I]** inferred from source + log jointly; **[O]**
open — needs the 052b fine timer seams in a probe run to settle.

**Headline correction to this plan's Context:** stage d ran with a
**device-resident CuArray pfield** — the log's stage-d banner says
`Wake particle field: device-resident CuArray (CUDA functional: NVIDIA
H200)` [V], and `fm052_common.sh` puts `VPM_ARRAYTYPE=cuarray` in
`FM052_GPU_ENV` for all GPU stages [V]. The Context bullet "host-backed
pfield is exactly why wake_sfs runs on CPU" is therefore WRONG for this
run, and P2's "requires CuArray-backed ParticleField in the driver loop"
is already satisfied in the 052 pressure driver (the 052b Phase A.1
shared-pfield work matters for the *multi-rotor ground* driver, not
here). Corroborating: the 8479d0f2 recenter!-SFS crash happened *in
stage d* — only possible if the SFS-armed RadixFMMCache was live in
that run; and the seam-handled SFS post hook
(`FLOWPanel_gpu_wake.jl:156-168`) hard-errors on a host pfield [V].

| # | interaction | algorithm / device as executed | timer category |
|---|---|---|---|
| 1 | particle self-UJ | SFS-armed radix GPU FMM (RadixFMMCache, device; `_gpu_device_pfield_target!` FLOWPanel_gpu_influence.jl:693-704) [I] | inside `influence_pass1` |
| 2 | Estr accumulation | on-device during #1's radix UJ (048 lifecycle); NOT a host `Estr_fmm!` walk in this config [I] | inside `influence_pass1` |
| 2b | SFS dynamic procedure | `pseudo3level` BeforeUJ + AfterUJ + clippings; the 29.5 s/step CPU cost lives here [I]; exact CPU-vs-device split [O] | `wake_sfs` (nested; wraps `pre_`/`post_evaluate_influence!` only — FLOWPanel_wake.jl:2083,2098 / FLOWPanel_gpu_wake.jl:164) |
| 3 | wake→panels + wake→probes | dense `direct_rectangular!` on GPU, pfield+panel-wake-rows as sources, fixed-small targets → linear in N [I] | `influence_pass1` |
| 4 | panel-wake rows→particles | dense rectangular on GPU (fixed-small sources, particle targets → linear in N) [I] | `influence_pass1` |
| 5 | panels→particles + panels→bodies/probes | dense rectangular on GPU [V per plan Context, unchanged] | `influence_pass3` / `body_influence` |
| 6 | panels→self (in-solve) | Backslash/S-gemv path, GPU S-gemv [V, unchanged]; velocity-pass panel influence recomputed per step, no AIC cache (`_sa_body_influence!` passes no plan_slot) [V] | `solve` / `body_influence` |

Counts [V]: `influence_pass1` fires 947× over 473 steps = **2/step**
(+1 warmup); `influence_pass3` and `solve` 473× = 1/step; `wake_sfs`
emitted per influence call, near-zero except once per step.

**Consequences for P2/P3 — SUPERSEDED (2026-08-26, later session): root
cause found.** The earlier hypothesis ("densification under the pinned
p=4/ncrit=50/θ=0.4 triple") was WRONG for the device path: the radix GPU
FMM does not use that triple at all. Its near field is governed by a
uniform grid of depth `ell` + near stencil `near_radius2`, derived ONCE
at first cache build by `_radix_auto_geometry`
(`FLOWVPM_fmm_radix.jl:481-503`) with an occupancy cap
`ell <= max(2, floor(log2(np)/3))` evaluated at the LIVE np — and the
cache is never rebuilt: `recenter!` preserves `ell`
(`_radix_fmm_evaluate!` :595-613) and FLOWPanel never calls
`clear_radix_fmm_cache!` (grep-verified, no call sites). Stage d's first
particle-UJ evaluation was at step 5 with **N=330** (wake-health CSV) →
**ell=2 frozen: a 4³=64-cell grid served 242k particles**. Quantified on
the step-472 snapshot (sweep script + log:
scratchpad `p3_sweep.jl`/`p3_sweep.log`, session 84b1b324; snapshot in
`data/052c_p3/`): near-field ordered pairs at (ell=2,q=6) = **91% of
dense** (5.3e10 of 5.9e10) — the device "FMM" ran essentially
brute-force, explaining pass1's N^1.58–1.69 and GPU≈CPU at matched N.
- Rebuild at plateau N helps but hits a second cap: the sigma-adequacy
  gate (`reach = 1.03·4.789·sigma_max = 0.098` vs box L=0.543) caps auto
  geometry at **(ell=4, q=19) = 26.3% of dense** — only 3.5× better.
- The adequacy cap is a **fat-tail artifact**: sigma quantiles
  [min,50,99.9,max] = [0.00104, 0.00424, 0.00807, 0.01978]; only **61
  of 241,986 particles** exceed half of sigma_max. At the 99.9%-ile
  sigma the rule would admit (ell=5,q=17) ≈ **1.2% of dense** — an
  order of magnitude, gated on handling ~61 oversize particles
  (side-channel dense leg, sigma clip, or level_radii2; science-
  affecting → Ryan's ruling, P2 scope).
- Octree sweep (CPU reference + host `Estr_fmm!` walk;
  SelfTuningTargetStop, shrink+recenter, p−1=3): pinned (ncrit=50,
  θ=0.4) = 6.56e9 direct pairs (11.2% of dense); (32,0.6) = 1.60e9
  (2.7%); (50,0.6) = 2.30e9 (3.9%). Full tables in `p3_sweep.log`.
  CAUTION for P3: `min_ncrit=50` exists "for SFS robustness"
  (`FLOWVPM_particlefield.jl:59`) — going below 50 needs a ruling.
- **Ruled (Ryan 2026-08-26, in-session):** fine-timer probe APPROVED
  (submitted, see below); frozen-ell fix APPROVED for local
  implement+test (done, see below); **I2, I4, I5 APPROVED**; I6 pending
  (Ryan asked for the rationale); consolidation: **keep gh200, h100,
  h200; retire l40s and b200**.

**Probe (approved) — submitted 2026-08-26:** h200 job **13494373**
(eng/qos=eng, started ~14:50 MDT immediately). Supplemental wrapper
`~rander39/fp052c_timer_probe_run.sh` on orc: exact mature-stage driver
invocation (warm restart 719, NREVS=19.5, 36 steps, production+GPU env)
with FLOWPANEL_STEP_TIMERS + FLOWPANEL_GPU_TIMERS + SAMPLE_INTERVAL=1
and FLOWPANEL_PARTICLE_FP64_KEEP=8, but NO gate/manifest machinery (the
deployed dirty sources fail the science-fingerprint reference gate by
design — this is a measurement, not a verdict; canonical manifests
untouched). Deployed to FLOWPanel-052-h200 via rsync --checksum: the 5
dirty src files (gpu_influence, simulate, solver, wake, warmstart) + the
driver. Run dir: `FLOWPanel-052-h200/data/fm052c_probe_timers_13494373`.
NOTE: the probe warm-starts at ~209k → its cache builds at ell=4
(adequacy-capped) — it measures the [O] splits, NOT the frozen-ell=2
pathology (cold-start runs only).

## Probe RESULTS — job 13494373 HARVESTED (2026-08-26 night)

Job COMPLETED (11m47s wall, exit 0; sacct). 36 mature steps 720–755,
warm restart from 719 at N≈209k, dirty FLOWPanel sources (P1.5 + fine
timers) — the radix cache built at step-720's live N → sane geometry
(sigma-adequacy-capped auto pick, not frozen ell=2). Raw log archived:
`data/052c_probe_timers/fp052c_timer_probe-13494373.out.gz` (parse by
script). Stats exclude step 720 (cache build + JIT: 77.8 s total,
wake_sfs 43.7 s one-time).

| category (nested indented) | mean s/step | min–max | chain 13484013 @ same N |
|---|---|---|---|
| **total_step** | **5.79** | 5.39–7.57 | ~51–66 |
| body_influence (= gpu pass3) | 2.79 | 2.78–2.80 | 2.8 |
| — rotor_panels_to_particles | 1.98 | flat | — |
| — panel_cross_targets | 0.82 | flat | — |
| monitors | 0.98 | 0.70–2.79 | ~1.1–3.5 |
| io (P1.5 fix live) | 0.69 | 0.53–1.10 | ~2.1–3.5 |
| solve (cached operator; host_ldiv 0.63) | 0.63 | 0.62–0.66 | 0.7 |
| **wake_influence** | **0.54** | 0.533–0.544 | **44.5–48.3** |
| — wake_sfs | 0.199 | 0.196–0.204 | 20.8–29.5 |
| — wake_to_rotor_panels | 0.127 | flat | — |
| — wake_to_probes | 0.100 | flat | — |
| — particle_self_uj (radix FMM) | 0.097 | flat | — |
| remaining_aerodynamics | 0.084 | | 0.8 |
| unclassified_residual | 0.000 | | — |

Settles every P1 [O] and the handoff questions:
- **pass1 radix-vs-dense split:** 2 calls/step = 0.090 + 0.331 s.
  particle_self_uj (the radix FMM UJ+Estr leg) is 0.097 s; the dense
  fixed-target legs (wake→panels 0.127, wake→probes 0.100) are linear
  and tiny. The chain's 14.8 s pass1 was ~entirely the frozen-ell=2
  near field.
- **wake_sfs BeforeUJ/AfterUJ split: moot.** wake_sfs = 0.199 s/step
  total — the chain's 20–30 s/step "CPU SFS cost" was the frozen-ell
  near-field walk, not the dynamic-procedure passes.
- **recenter! count (I4):** recenter!/rebuild emits no log line
  (grep-verified in FLOWVPM+FastMultipole src), so the count is
  inferred from timing flatness: wake_influence spread over 35 steps is
  0.533–0.544 s with no ~0.95 s spikes → **0 events in 36 steps**,
  consistent with I4's ~1-per-96-steps estimate. I4's recommendation
  (skip in-place recenter!) HOLDS.
- **Pair-count ≠ GPU time.** The step-472 sweep's "ell=4 = 26% of
  dense" pair count predicted only 3.5× over the chain; measured is
  ~35× (14.8 → 0.42 s). ell=2's 64-cell grid also destroyed GPU
  parallelism, not just pair counts. Consequence: the **sigma fat-tail
  lever (ell=5 via 99.9%-ile) is NOT needed for acceptance** — 0.42
  s/step leaves nothing worth an order-of-magnitude on. Recommend:
  defer (optional later optimization); the oversize-particle ruling can
  wait.
- **Budget at peak N (I2):** wake terms ~0.54 s at 209k; even at
  N^1.7 to a ~260k peak that is <0.9 s. The floor terms now dominate:
  body_influence 2.8 (linear-in-N panels→particles 2.0) + monitors 1.0
  + io 0.7 + solve 0.6 ≈ 5.1 s. Peak-N step estimate ~6.5–7 s/step →
  1080 steps comfortably inside 4 h (early steps far cheaper).

**Re-scope consequence (proposed, needs Ryan):** P2's all-FMM routing /
device-residency restructure is unnecessary — the pipeline as-is (with
the P1.5 io fix + fine timers, both in the dirty FLOWPanel worktree)
meets the target once the cache geometry tracks N. The acceptance path
collapses to: (1) deploy the FLOWVPM depth-rebuild fix (cold-start runs
build at N=330; the fix rebuilds as N doubles), (2) 36-step mature gate
per P4, (3) I5 near-peak probe + checkpointed 1080-step acceptance.
P3's octree triple retune now only affects the CPU reference / host
Estr walk cost, not the GPU acceptance — decide separately (I6).

**Frozen-ell fix (approved, implemented locally 2026-08-26):**
`FLOWVPM_fmm_radix.jl` — new `_radix_depth_outgrown!` +
`_radix_fmm_coupling!` now drops/rebuilds the coupling when
`_radix_auto_geometry` at the current np/bounds/sigma picks a strictly
deeper ell (cheap host guards: recheck only after np doubles, and only
when the occupancy cap has passed the cached ell; user-fixed ell never
rebuilds; inadmissible-geometry keeps the cache). Regression testset
"depth rebuild on particle growth (052c)" added to
`test/runtests_gpu_fmm.jl` (grow 300→8000 at fixed sigma: expects
ell 2→4 rebuild, accuracy vs UJ_direct, no churn at fixed np, fixed-ell
promise kept). GPU-path-only; CPU reference arithmetic untouched.
Uncommitted, in the FLOWVPM worktree.
**VERIFIED (same session):** new testset **6/6 green** standalone
(ell0=2 → rebuilt deeper, u_rel_rms=3.1e-5 vs 1e-3 gate; scratchpad
`052c_depth_test.{jl,log}`); the suite's first two testsets also pass
with the fix (6/6, 12/12). CAVEAT — **pre-existing, unrelated failure**:
the suite's "task-035 tuning settings (host path)" testset errors at
first cache build (`HierarchicalRigidStencil has no M2L level ...
near_radius2=12 at ell=2, ell_axes=(1,1,2)`; rectangular wake n=200)
and aborts the file before later testsets. Reproduced with the 052c fix
fully reverted (clean tree, same error — logs
`052c_radix_test_clean.log` vs `052c_radix_test.log`), so it is NOT 052c breakage.
**TRIAGED (2026-08-26 night, source-analysis agent):** not a
FastMultipole 8479d0f2 regression either — the validation code is
unchanged since 2026-08-13 (`c8748182`). Trigger = FLOWVPM commit
`4494c25` (2026-08-24), which added this test block AND bumped the
partitioned `rho_t` default 3.668→4.789 in the same commit. At
rho_t=4.789 the wake-200 auto geometry needs stencil gap ≈1.79 leaf
units → near_radius2 snaps 6→12; on the 2×2×4 rectangular leaf grid
every pair is then inside the near ball (max |o|²=11 ≤ 12), so the
hierarchical stencil correctly has zero M2L levels and throws
(`interaction_list_batched.jl:297-311,376-383`). The test was never
valid at the defaults it ships — geometrically true, not a validation
bug. **RULED + IMPLEMENTED (Ryan 2026-08-26 night): (a) testset pin AND
(b) zero-M2L degenerate support** (Ryan: small fields where pure direct
is most efficient are legitimate). Correction to the triage's suggested
pin: `near_radius2=6` would FAIL the per-step regularized adequacy gate
(`g_min·h_leaf = √2·1.49 ≈ 2.1 < rho_t·sigma_max ≈ 2.58` — exactly why
auto snapped to 12), so the pin is the resolved degenerate config made
explicit: `radix_fmm_settings!(atomic_field; rectangular=true, ell=2,
near_radius2=12)` + a pure-direct accuracy check vs `UJ_direct`.
Zero-M2L support (FastMultipole, uncommitted): a hierarchical cache
whose grid has every leaf offset inside the near ball
(`first_m2l_level == ell+1`) now builds with an empty push union/
schedule and evaluates pure direct instead of throwing. Audit found the
lifecycle already degenerates safely (empty level loops, zero-block
launch guards, `root_level==ell` node builds, graph capture reduces to
the locals memset); the hard breaks fixed: degenerate early-return in
`_hierarchical_scheduled_tables` (explicit `level_radii2` on such a
grid stays an ArgumentError), `maximum(...; init=0)` for
`max_level_nodes`, `isempty(cache.accepted_offsets)` short-circuit in
`_direct_kernel_geometry_gate!`, device window-stride clamp
`K = max(min(window_classes, noffsets), 1)`, and a degenerate-aware
message in FLOWVPM's `_validate_radix_fmm_settings` mirror. New
host regression: `test/radix_trimming_test.jl` section "(g) zero-M2L
degenerate geometry" (first_m2l==ell+1, n_routes==0, exact-once
direct coverage, ≈direct-sum accuracy, schedule rejection). Device
path is guard-audited but not locally testable (no GPU).
**VERIFIED (2026-08-26 night): all green.** FastMultipole
radix_trimming_test.jl 131/131 (incl. new section g),
hierarchical_m2l_host_test.jl 847/847. FLOWVPM runtests_gpu_fmm.jl
runs to completion for the first time since the rho_t bump: 228/228
across all 9 testsets — task-035 100/100, depth-rebuild-052c 6/6,
rectangular/recenter/SFS-host all pass (log:
session-ac512cfe scratchpad flowvpm_gpu_fmm.log). The
`_radix_auto_geometry` admissibility hardening is now moot — a
degenerate auto pick is a valid configuration.

## P0 / P1.5 status (2026-08-26, updated after Ryan's rulings)

- **P1.5 REVISED (Ryan 2026-08-26, later): single series, no sidecar.**
  Ryan ruled the dual-write (F32 visual + F64 sidecar) was a useful
  debugging/restart-verification stage but users need one OR the
  other. Implemented (uncommitted): `write_vtk` now writes ONE
  uncompressed particle series at `FLOWPANEL_PARTICLE_PRECISION`
  ("f64" default = pfield eltype, replay-exact restarts; "f32" =
  half the write/disk, loader warns restarts are not replay-exact).
  Sidecar generation + `FLOWPANEL_PARTICLE_FP64_KEEP` pruning removed
  (the env var is now a no-op; wrappers that still set it are
  harmless). The warm-start loader KEEPS its read-only sidecar
  preference so runs written during the dual-write era (incl.
  acceptance 13497184, in flight on the old code) restart fine.
  Verification in progress (warmstart + simulate suites).
  The paragraph below describes the superseded dual-write design.
- **P1.5 COMPLETE (Ryan-ruled "do it"):** the particle .vtp write site
  (FLOWPanel_wake.jl, `_write_particles_vtp`) now writes the visual
  series **Float32 + compress=false** AND a full-precision uncompressed
  checkpoint sidecar (`<wake>_particles_fp64/…$idx.vtp`, same schema, no
  PVD). The warm-start loader prefers the sidecar and warns loudly if it
  must fall back to a Float32 visual file (pre-052c runs with F64
  visuals load exactly as before). Disk bound for long runs:
  `FLOWPANEL_PARTICLE_FP64_KEEP=k` retains only the last k checkpoints
  (default 0 = keep all — set it, e.g. 8, in production stage-d runs;
  keep-all doubles disk vs the old format). Verified: warmstart 153/153
  (1e-10/1e-11 replay exactness via the sidecar), simulate 199/199, no
  fallback warnings. Expected io: visual write ~0.1 s + sidecar ~0.2 s
  ≈ 0.3 s/step at 230k (vs ~2.0 before). This also gives P4/I5-style
  restartability full-precision restart points by construction.
- **P0 sync DONE and verified:** all four kept trees
  (`orc:FastMultipole-052-{gh200,h100,h200,l40s}`) carry both 8479d0f2
  files (marker counts src=4/test=1 confirmed per tree, 2026-08-26).
  Correction: an earlier session note claimed the fix was "missing from
  all five trees" — that probe grepped a test-file marker against the
  src file (false negative). b200 tree left untouched (dropped).
  Checkout-consolidation decision + notebook entry still pending Ryan.
- **Retiree audit (2026-08-26 night; ruling: keep gh200/h100/h200,
  retire l40s+b200):** all orc 052 trees are plain rsync deployments
  (no .git anywhere). Source diff vs kept trees: FLOWPanel-052-{l40s,
  b200} byte-identical to h100 (h200 differs only because it received
  the probe's dirty-source deploy); FLOWVPM-052-{l40s,b200} identical
  to h200; FastMultipole-052-l40s identical to h200;
  FastMultipole-052-b200 differs only by being STALE (never received
  the 8479d0f2 P0 sync). **No unique source work.** Unique data: only
  `FLOWPanel-052-l40s/data/fm052_multiarch/l40s/` (74K — job 13477880
  compatibility-probe artifacts + manifests, absent from h100's copy);
  b200's multiarch dir is empty. NOT deleted — per instruction,
  reporting instead: recommend copying that 74K into
  FLOWPanel-052-h100/data/fm052_multiarch/ then removing all six
  retiree dirs.

## Rulings + execution (2026-08-26, post-probe session)

**Ryan ruled (2026-08-26):**
- **Re-scoped acceptance path APPROVED in full**: deploy fixes to the
  h200 stack, dirty FLOWPanel sources (P1.5 + fine timers) become
  canonical for it; 36-step mature gate → I5-style near-peak probe →
  checkpointed 1080-step acceptance. Each sbatch shown to Ryan before
  submission.
- **P3 SKIPPED** (pinned CPU reference stays as-is; min_ncrit<50 moot).
- **Retiree cleanup APPROVED and EXECUTED**: the unique 74K
  `FLOWPanel-052-l40s/data/fm052_multiarch/l40s/` was rsynced into
  `FLOWPanel-052-h100/data/fm052_multiarch/` (diff -r verified
  identical), then all six l40s/b200 trees deleted. Remaining orc 052
  trees: {FastMultipole,FLOWVPM,FLOWPanel}-052-{gh200,h100,h200} +
  FLOWPanel-052 + FLOWVPM-046 (chain stack).
- **I1-monitors — SUPERSEDED same day:** after Ryan asked whether the
  wake monitors really cost ~1 s/step, investigation confirmed the
  driver already env-gates them (`WAKE_HEALTH`,
  `WAKE_HEALTH_ATTRIBUTION`, `WAKE_INVENTORY`, master `RUN_MONITORS`),
  and Ryan ruled: turn them off by default and for this item.
  CONSTRAINT found first: `fm052_compare.jl` reads per-step
  `n_particles` from `monitors/wake_health.csv` for the count_tol gate
  and errors if the CSV is missing → `WAKE_HEALTH` must stay ON for
  gated 052 runs (kept default true). IMPLEMENTED: driver defaults
  flipped to OFF for `WAKE_HEALTH_ATTRIBUTION` (extra O(np) pass +
  sort; appended-after columns, nothing in the gates reads them) and
  `WAKE_INVENTORY` (O(np) binning + quantile sorts; its CSV is not
  read by any gate) — uncommitted in the FLOWPanel worktree, deployed
  to FLOWPanel-052-h200. The running mature-gate job 13496946 started
  before the flip (monitors all-on, harmless — diagnostic-only, no
  science effect); future runs (near-peak probe, acceptance) pick up
  the new defaults. Sigma
  fat-tail lever: not ruled on this round; remains deferred by default
  (nothing selected, no action needed for acceptance).
- **Commit plan**: Ryan wants a proposal before anything is committed.
  Notebook draft: not requested this round.

**Executed (this session):**
- h200 deploy DONE via rsync --checksum: FastMultipole-052-h200 (3 src
  + 5 test files), FLOWVPM-052-h200 (FLOWVPM_fmm_radix.jl +
  runtests_gpu_fmm.jl), FLOWPanel-052-h200 (driver; the 5 dirty src
  files were already identical from the probe deploy).
- Mature-gate wrapper staged at `orc:~/fp052c_mature_gate_run.sh`:
  exact stage-c invocation (production env + SAMPLE_INTERVAL=1 + fine
  timers + FP64_KEEP=8) from the h200 stack (FPDIR/VPMDIR/ENVDIR =
  *-052-h200 / fm052env-h200), then inline source gate (36 GPU-S / 0
  CPU-S), `fm052_compare.jl verify 720 755`, and `fm052_gate.sh` with
  the chain's locked tolerances + pinned CPU reference
  (`FLOWPanel-052/data/{fm052_campaign_lock,fm052r_cpu_mature_pinned}`),
  FP052_COUNT_TOL=16. Awaiting Ryan's go to sbatch (eng/qos=eng,
  h200:1, 64 cpu, 192G, 1 h — probe completed in 12 min).

## Mature gate PASSED + monitor findings (2026-08-26, later)

- **Mature gate job 13496946 PASSED** (eng/qos=eng started immediately;
  COMPLETED 11m51s, exit 0): all locked correctness gates (CT
  cycle-mean diff 4.9e-6 abs / 6.9e-5 rel; Γ rms normalized diff
  5.4e-5), count gate at 209,581 final particles, source paths 36
  GPU-S / 0 CPU-S, memory gate passed. Timing: total_step mean 7.66
  s/step incl. the 77 s first-step cache-build+JIT; ≈5.7 s/step
  excluding it (min 5.60) — consistent with probe 13494373. Artifacts:
  `FLOWPanel-052-h200/data/fm052c_mature_gate_13496946/`. The deployed
  stack (depth-rebuild + zero-M2L + P1.5 + timers) is validated.
- **Monitor rulings (Ryan, same day):** only steady Bernoulli — already
  the case (BERNOULLI_ONLY=true → pressure_bernoulli +
  force_monitor_bernoulli + bound_circulation; the Laplace pairs never
  ran in 052). Driver defaults flipped OFF for WAKE_HEALTH_ATTRIBUTION
  and WAKE_INVENTORY (uncommitted, deployed to h200). WAKE_HEALTH
  stays ON — fm052_compare.jl reads monitors/wake_health.csv
  n_particles for the count gate.
- **Monitors bottleneck REFUTED by microbenchmark** (scratchpad
  `wakehealth_bench.{jl,log}`, 240k synthetic particles, local 1
  thread): `_wake_health_stats` 2.1 ms/call, `_wake_health_attribution!`
  4.5 ms/call — the wake scans are NOT the ~1.1 s/step monitors
  category (gate run: monitors mean 1.11, min 0.78). Remaining
  candidates: the Bernoulli pressure+force pair itself, per-monitor
  D2H mirror syncs acting as GPU sync points (would also explain the
  chain's apparent N^0.72 growth), CSV appends, verbose prints. The
  acceptance run (attribution+inventory off) will discriminate: if
  monitors stays ~1 s, the wake scans were never the cost. No further
  action needed for the 10 s target either way.
- **Acceptance wrapper staged** at `orc:~/fp052c_accept_run.sh`:
  (A) I5 near-peak probe — 30-step warm restart from the chain's
  step-472 checkpoint (N≈242k; NREVS=12.47 → steps 473..502; verified
  present in `FLOWPanel-052/data/fm052d_gpu_1080/`), then (B) exact
  1080-step cold stage-d acceptance (NREVS=28.5, run_name
  fm052d_gpu_1080 in the h200 tree, FP64 sidecar KEEP=8, fine timers)
  + canonical stage-d checks (source gate 1080/0, n_steps metadata,
  report, verify 0 1079). Proposed submission: eng/qos=eng, h200:1,
  64 cpu, 192G, 12 h. NOT yet submitted.

## Per-monitor cost profile (2026-08-26, Ryan-requested)

- Ryan wants the cost of each major timestep part; the only opaque
  category was `monitors` (1.1 s/step). NOTE: `_step_timer_sync()`
  (FLOWPanel_instrumentation.jl) calls `CUDA.synchronize()` at every
  timer boundary, so category timers CANNOT absorb GPU tail latency
  from earlier categories — the earlier "sync absorption" hypothesis
  is wrong for instrumented runs; the 1.1 s is genuinely inside the
  monitors loop.
- IMPLEMENTED (uncommitted, deployed to h200): nested per-monitor
  timers `monitor_<Type>` / `monitorcsv_<Type>` in the monitors loop
  (FLOWPanel_simulate.jl) + `d2h_mirror_sync` nested timer in
  `_wake_monitor_host_pfield` (FLOWPanel_gpu_wake.jl). Local
  `using FLOWPanel` load check passed.
- Profile job **13497199** submitted (eng/qos=eng, 40 min): 6
  mature-wake steps (restart 719, NREVS=18.67 → steps 720–725), ALL
  monitors armed (WAKE_HEALTH_ATTRIBUTION=true WAKE_INVENTORY=true
  forced, to also price the two now off by default). Run dir
  `FLOWPanel-052-h200/data/fm052c_monitor_profile_13497199`.
- Acceptance job **13497184** (near-peak probe + 1080-step stage d)
  was already running before these edits — it profiles nothing new but
  is unaffected (sources loaded at its startup).

## Session 3 state (2026-08-26 late night — context-reset handoff)

**Acceptance job 13497184 FAILED at step 502** (near-peak probe stage,
9m19s in): FastMultipole's regularized-adequacy gate
(`_direct_kernel_geometry_gate!`, translate_batched_resident.jl:2104)
correctly refused the cached ell=4 grid once merge-grown sigma_max
(0.0198 at build → 0.02137 by step 502) exceeded `g_min·h_leaf/rho_t`.
GOOD news from the same job: steps 473–501 ran at **~6.3 s/step at
N≈242k** (near-peak, under target). Ryan asked about disabling merging
instead: assessed as a science-fingerprint change (pinned CPU reference
+ locked tolerances were generated WITH `MERGE_R_FACTOR=0.0052`; np
compaction also lost) — recommended AGAINST for acceptance; geometry
fix chosen.

**Fix IMPLEMENTED (uncommitted, FLOWVPM worktree, NOT yet verified or
deployed):** `_radix_sigma_limit` + `_radix_sigma_outgrown!` in
`src/FLOWVPM_fmm_radix.jl` — coupling reuse now checks live
`_radix_sigma_max` (O(np) device reduce, every call) against the cached
geometry's admissible limit `g_min·h_leaf/(accuracy_margin·rho_t)`
(Inf for zero-M2L degenerate); on breach the coupling rebuilds via
`_radix_auto_geometry` at the grown sigma (build margin = headroom, so
rebuild fires before the hard gate throws). Fixed-ell = promise (gate
error propagates); inadmissible-everywhere keeps the cache. st tuple
gained `sigma_limit` field. New testset "radix FMM coupling:
sigma-outgrown rebuild (052c near-peak)" in test/runtests_gpu_fmm.jl
(rebuild admits new sigma, accuracy vs UJ_direct, no churn, fixed-ell
@test_throws). **A julia-test-runner agent was mid-run on the full
runtests_gpu_fmm.jl suite at handoff — check
scratchpad/flowvpm_sigma_rebuild.log for the verdict (expect 10
testsets; prior count was 228 passes + the new set).**

**Also this session (all uncommitted, VERIFIED green 153/153 warmstart
+ 199/199 simulate):** single-series particle io — sidecar REMOVED.
`write_vtk` writes ONE uncompressed .vtp series at
`FLOWPANEL_PARTICLE_PRECISION` (package default f64 = replay-exact
restarts; f32 = half io/disk, loader warns non-replay-exact).
`FLOWPANEL_PARTICLE_FP64_KEEP` is now a no-op; loader keeps READ-ONLY
sidecar preference for dual-write-era runs. Ryan ruled: **052 launcher
defaults to f32** (env-default block added at top of
`rotor_hover_pressure_comparison.jl`), package default f64 =
production. NOTE: an f32 mid-run restart of the acceptance is not
replay-exact (gates compare monitor CSVs/tolerances, should still
pass).

**Per-monitor profile HARVESTED (job 13497199 COMPLETE, 5 mature steps
at 209k, ALL monitors forced on; log
`FLOWPanel-052-h200/data/fm052c_monitor_profile_13497199.log`):**
monitors total 0.87 = WakeHealth+attribution **0.533** + WakeInventory
**0.295** + d2h_mirror_sync 0.054 + CSVs ~0.04 + Bernoulli/Force/
BoundCirc ≈0. The wake scans ARE the cost on the Xeon host (the local
M-series microbenchmark underestimated ~100×: strided 45-row gathers).
Default-off flips (attribution+inventory) were the right call →
monitors expected ~0.2–0.3 in production config. Nested extras now
visible: rotor_panels_to_particles 1.98, panel_self_cached_operator
1.01, host_ldiv 1.01, panel_cross_targets 0.82, total_step 6.01.
Remaining cost-reduction targets are the flat panel legs (AIC/
panels-on-self precompute ideas, P2 bullet 3 — unruled).

**Deployed to h200 trees this session:** per-monitor+d2h nested timers
(FLOWPanel_simulate.jl, FLOWPanel_gpu_wake.jl). **NOT yet deployed:**
the sigma-outgrown fix (FLOWVPM), single-series io (FLOWPanel_wake.jl,
FLOWPanel_warmstart.jl), f32 launcher default (driver).

**Immediate next steps:** (1) read the suite verdict in
scratchpad/flowvpm_sigma_rebuild.log — if green, rsync --checksum
FLOWVPM src/FLOWVPM_fmm_radix.jl + test/runtests_gpu_fmm.jl →
orc:FLOWVPM-052-h200/, FLOWPanel src/FLOWPanel_wake.jl
src/FLOWPanel_warmstart.jl examples/rotor_hover_pressure_comparison.jl
→ orc:FLOWPanel-052-h200/; (2) resubmit the SAME acceptance sbatch
(wrapper `orc:~/fp052c_accept_run.sh`, eng/qos=eng h200:1 64cpu 192G
12h, output pattern fp052c-accept-%j.out — Ryan already approved and
said go once; resubmission after the fix is the approved path); (3)
arm a single scheduled sacct check (~2.5 h); (4) on success, harvest
verdicts + timing, then the still-open queue: commit plan (proposed in
plan doc region above, Ryan wants it before committing; suggested
trigger was gate pass — now also fold in: sigma fix, single-series io,
f32 launcher default, per-monitor timers), 052/052a closure notebook
entry (NOT yet drafted — needs Ryan approval before any notebook
write). Mature gate 13496946 PASSED earlier (see section above) — the
new FLOWVPM/FLOWPanel edits post-date it; the acceptance's own gates
cover them, but a re-run of the 36-step mature gate wrapper
(`orc:~/fp052c_mature_gate_run.sh`) after deploy is the conservative
option if Ryan wants it.

## Constraints / operational (carried from 052a HANDOFF 6)
- Frozen-tree constraint LIFTED (b200 dropped, 13477878 cancelled) —
  consolidation is P0 above.
- All commits LOCAL/unpushed; never push without Ryan's say-so.
- No polling: single scheduled checks at scheduler-estimated times
  (Monitor tool, ≤1 h each; background Bash dies past ~10 min). Strip
  ANSI from orc output (`sed 's/\x1b\[[0-9;]*m//g'`). slurm-availability
  skill for queue choices. sacct authoritative. H200 via qos=gpu (m13h)
  or qos=eng; no standby route; twin-and-cancel-loser approved.
- sbatch/scontrol over ssh may hit the permission classifier — if
  blocked twice, hand Ryan the exact command with the `!` prefix.
- Local runs: ≤4 threads. Never read data/*.csv directly — script a
  summary.

## Milestone status (2026-08-26)
- 052: CLOSED (Ryan). All phases done except the 1080-step acceptance,
  which this item now owns (P4). h200 chain 13484013 proved the
  recenter!-sfs fix (8479d0f2) and passed stages a–c + mature gates
  before timing out on pace.
- 052a: CLOSED (Ryan). gh200 COMPLETE, l40s CLOSED (ineligible by
  design), h100 PASSED all gates (13477876), b200 DROPPED (13477878
  cancelled before start). Consolidation + notebook entry = P0 here.
- 13484013 CONFIRMED TIMEOUT at 08:16:55 MDT (04:00:16 elapsed) at step
  473/1079, ~66 s/step in the final block. Final log harvested; all
  data/052c_phase1/ artifacts refreshed to the full 473 steps
  (2026-08-26 09:04). Nothing left to harvest.

## Session 4 state (2026-08-26 night — context-reset handoff)

**Sigma-outgrown fix VERIFIED + DEPLOYED + COMMITTED; acceptance rerun
13497273 then FAILED DEEPER at step ~1071 — rVPM core collapse, NOT a
code bug.** Full diagnosis + trial ledger:
`052c-sigma-experiments-2026-08-26.md` (READ IT). Short version:
unguarded Euler core update `sigma -= dt*sigma*Z` sign-flips when a
strained OUTLIER hits dt·Z > 1 (dt·Z = 1.24 @1014 → sigma < 0 @1015;
second event dt·Z = 164 @1024; p1 sigma percentile flat at 0.32
throughout = population healthy; probe job 13501467, restart 990→1031,
DTZ+ATTRIBUTION armed, COMPLETED). Crash mechanism: negative sigma →
Brent bracket (0, negative) in `solve_ρ_over_σ` during
BoundCirculationMonitor's host direct!. Steps ≥1015 were never-before-
simulated territory (mature gate + pinned CPU ref end at step 755).

**Trial 1 (Ryan-ruled): cap dt·Z at 0.5 + absolute floor 1%·σ_shed,
kwarg-gated (Ryan: "gated by kwargs so we can easily try different
approaches").** Implemented: `sigma_guard::NamedTuple` kwarg chain
simulate! → propagate! → FLOWVPM._euler → both reformulated Euler paths
(`_sigma_guard_params`, FLOWVPM_timeintegration.jl; keys :dtz_cap
:floor, unknown keys throw, empty = bit-exact legacy; ClassicVPM/
euler_exp reject non-empty; warmstart replay forwards it). Driver env:
SIGMA_DTZ_CAP / SIGMA_FLOOR_FRAC (default off), echoed in the log
banner. VERIFIED: FLOWVPM gpu_fmm 244/244 (new "euler sigma_guard"
testset 9/9), FLOWPanel warmstart 153/153, simulate 199/199. DEPLOYED
(rsync --checksum) to FLOWVPM-052-h200 (timeintegration, tests) +
FLOWPanel-052-h200 (wake, warmstart, simulate, driver). UNCOMMITTED:
these trial-1 files in FLOWVPM+FLOWPanel worktrees, the ledger, the
052d docs, and MATRIX_OPERATOR_REFACTOR/scripts/fp052c_trial1_run.sh +
fp052d_sigma_probe_run.sh (wrapper copies; also at orc:~/).

**IN FLIGHT: chain job 13501691** (eng/qos=eng h200 6 h, submitted
~21:55 MDT 2026-08-26; wrapper orc:~/fp052c_trial1_run.sh; log
orc:FLOWPanel-052-h200/data/fp052c-trial1-13501691.out): stage 1 =
36-step mature gate GUARD ARMED vs pinned CPU ref (expect bit-identical
— guard shouldn't engage ≤755; set -e aborts chain on failure); stage
2 = 1080-step acceptance run_name **fm052d_gpu_1080_t1** guard armed
(unguarded baseline dir fm052d_gpu_1080 PRESERVED for comparison) +
canonical stage-d checks. A ~55-min Monitor check was armed but dies
with the session — on resume, check sacct for 13501691 and re-arm
single scheduled checks (expect gate verdict ~15 min in, acceptance
done ~3 h total). Harvest into the ledger's "Results:" slot: gate
bit-identity, min_sigma trajectory (expect plateau ≥ 4.45e-5, no
crossing), max_u tail, wake_health tail, CT/Γ/count gate outcomes,
monitors timing (also confirm the ~0.2–0.3 s/step monitors prediction
— still unconfirmed from a full run).

**COMMITTED this session (all local/unpushed):** FastMultipole
d7251cc5 (zero-M2L + gate hardening) + 9cdbc27a (plan docs/scripts);
FLOWVPM d871d47 (depth + sigma-outgrown rebuilds); FLOWPanel b6431c4
(shared Backslash), 4155f32 (single-series io), 1beb297 (nested
timers), 07daf8d (driver defaults f32 + monitor flips). Remaining
FLOWPanel dirty files belong to other items — left uncommitted.

**052d (FMM for panel influence): plan written, NOT approved, NOT
implemented** — `052d-plan-2026-08-26.md` + Ryan-added companions
`052d-plan-review-2026-08-26.md` (shared-world-grid architecture
proposal) and `052d-plan-review-response-2026-08-26.md` (its staging
supersedes the plan's implementation steps PENDING Ryan's ruling; note
corrections: body→particle leg is U-only in production, design target
is 4 rotors). Do not start 052d work without the ruling.

**Open queue:** (1) trial-1 harvest → ledger results + next-trial
ruling if needed; (2) commit trial-1 + ledger + 052d docs once Ryan
approves the trial (commit messages follow this session's pattern);
(3) 052/052a closure notebook entry — NOT drafted, needs Ryan approval
before any notebook write (offer notebook-drafter); (4) 052d ruling;
(5) deferred idea: thin WakeHealth mode (np-only CSV) — only if
trial-1 timing shows the base scan matters.

**Operational (carried):** local ≤4 threads; delegate runs to
julia-test-runner with scratchpad logs; never read data/*.csv directly
(awk/script summaries fine over ssh); ssh orc needs bash -lc, strip
banners, keyboard-interactive auth EXPIRES mid-session (only Ryan can
re-auth: `! ssh orc echo ok`); sacct authoritative; single scheduled
checks ≤1 h each (Monitor tool), no polling; sbatch may hit the
permission classifier — retry once, then hand Ryan the command with
`!`; FLOWPanel/FLOWVPM suites: JULIA_DEPOT_PATH=/private/tmp/
fp052b_julia_depot:/Users/ryan/.julia, julia --project=<FLOWPanel.jl
dir>, run from a temp dir; eng/qos=eng starts immediately on H200
(verified 4× today); NEVER commit/push/submit without Ryan's
instruction (trial-1 chain + its re-arm on failure-free resume are
already approved).
