# Plan — Item 051: panel–particle GPU coupling (plan from docs; audit existing worktree code as untrusted)

> **Handoff note:** per plan-mode constraints this file lives at
> `~/.claude/plans/start-item-051-in-vivid-lobster.md`. **First execution step:** copy it to
> `MATRIX_OPERATOR_REFACTOR/051-plan-2026-08-22.md` so it sits with the other phase-P docs.

## Context

049 and 050 are CLOSED/approved (handoff-phaseP-2026-08-22.md, "fifth session" section is
authoritative). 050's verdict B′ (theory/panel-multisystem-scoping.md): keep FLOWPanel's 3-pass
`influence!` structure; add **rectangular GPU brute-force cross-pass kernels beside the radix
framework** (radix stays targets===sources, untouched); panel self-solve via device-resident dense
`NearfieldInfluenceCache` matvec **only if it prices in** (36,752² F64 ≈ 10.8 GB, ~3.6 ms/matvec).
Budget: pessimistic stack 0.373 (particles, D15 worst) + 0.04 (wake→panels) + 2.0 (panels→particles)
= 2.41 s of 3.3 s/step → **0.89 s solve headroom ≈ 246 matvecs**. Strategic direction: don't
foreclose eventual multi-system radix generalization.

**051's four stages** (051-impl-panel-particle-gpu-coupling.md:29–64):
1. Rectangular device kernel, gaussianerf points→arbitrary targets (041k-tiled clone, separate
   source/target arrays, F64 primary + F32/rsqrt opt-in), wired as wake→bodies in pass 1.
2. Rectangular device kernel, panels→arbitrary targets (port of FLOWPanel `direct!`,
   FLOWPanel_abstractbody.jl:1264 — constant source/doublet tris + vortex-ring filaments); retires
   the ~36 s CPU body-pass floor.
3. Solve: **measure first** (FGS/Krylov iteration counts + nearfield-cache matvec cost at production
   shape), then device matvec only if it prices in.
4. Timing report vs the 049 budget (feeds 052). FmmPlan/NearfieldInfluenceCache reused as-is on the
   CPU side; RadixFMMCache untouched.

**Gates** (051 doc:58–64, 96–98): pass-by-pass CPU-vs-GPU parity at the 018 operating point
(36,752 panels / ~210k particles), phase gate 1e-3 with F64 tighter where applicable; FLOWPanel CPU
test suite green + FastMultipole/FLOWVPM unregressed; harness must run the **full step head**
(`maneuver!` + reset/freestream/kinematic + `update_TE!`) before any influence eval (skipping it
silently NaNs every target — cost 3 jobs previously).

### Critical state directive (user decision, 2026-08-22)

The worktree is ahead of the handoff: FastMultipole `1cd0b98a` and FLOWPanel `d6bf8b6` (both
2026-08-22 ~15:05–15:13) claim "rk3 step in 300ms on gpu", and 051-shaped code already exists.
**User directed: the handoff is strictly authoritative — plan 051 from the docs; treat ALL of the
following as untrusted work-in-progress; reuse only after component-by-component audit; the "300ms"
commits carry no evidentiary weight** (the CUDA kernels are explicitly marked "WRITTEN BLIND …
parse-checked only"):

| Untrusted artifact | Where |
|---|---|
| Host rectangular kernels + functors (`RectangularGaussianErfVortex`, `RectangularPanelInfluence`, `direct_rectangular!`, `rect_source_rows`) | FastMultipole `src/direct_rectangular.jl` (778 lines, committed; included at `src/FastMultipole.jl:102`, exported `:115`) |
| Blind-written CUDA kernels (`_cuda_rect_points_kernel!` etc., `_RECT_TILE_POINTS=256`, launch wrappers) | FastMultipole `src/translate_batched_cuda.jl:8955–9126`; shared pair math `:213–248`, vendored fdlibm erf `:169–200`, arg validation `_rect_check_args` `:653–665` |
| Host-only test file (no CuMatrix cases) | FastMultipole `test/direct_rectangular_test.jl` (457 lines) |
| FLOWPanel influence seam (env `FLOWPANEL_GPU_INFLUENCE` / `set_gpu_influence!`, pass recognition, fallbacks incl. Estr-stays-on-radix) | FLOWPanel `src/FLOWPanel_gpu_influence.jl` (untracked, 673 lines) + hook in `FLOWPanel_fmm.jl:74–79` (uncommitted mod) |
| 052-scope device-resident-wake seam — **out of 051 scope, leave untouched** | FLOWPanel `src/FLOWPanel_gpu_wake.jl` (untracked) |
| Synthetic rect-kernel bench (both passes at 018 shape, F64/F32, 2000-target sample, 1e-11 F64 gate) | FLOWVPM `scripts/fm051_rect_bench.jl` (committed) + `fm051_submit.sh` / `fm051_run.sh` |
| Many uncommitted FLOWPanel `src/*.jl` modifications (simulate, wake, solver, fmm, elements_fmm, …) | FLOWPanel worktree — audit which are load-bearing for 051 vs unrelated |

Intentional uncommitted changes to preserve: FLOWVPM `scripts/fm049_rotor_verify.jl` rewrite
(multi-snapshot 710–719, SHA-256 manifest, D14 pinned; particle-only). Note: the "uncommitted
tree.jl Float32 fix" is actually **committed** as `bd90a97e` — nothing to preserve there.

## Authoritative constraints (do not violate)

- Production defaults **D14** (P=6, `_PARTITIONED_RHO_T_DEFAULT=4.789` in FLOWVPM
  `src/FLOWVPM_fmm_radix.jl:79,:193`) + **D15** (upload-per-step) — never silently change.
- Cluster: `ssh orc`, trees `~/FLOWVPM-046` + `~/FastMultipole-046`, env `~/fm048env`
  (Julia 1.11.7 module `julia/1.11.7-6bmogfl`, CUDA.jl 6.3.0), normal H200 QoS only.
  **User submits all jobs — provide commands only.**
- ≤4 local threads. Token discipline: `julia-test-runner` for all runs, `refactor-docs-librarian`
  for MATRIX_OPERATOR_REFACTOR docs, redirect long output to scratchpad logs.
- Watch item: the **2.6e-4 CPU-vs-GPU direct cross-check discrepancy** from 049
  (049 doc:178–184; possibly already fixed by commit `baf8fb3` per 049 doc:276) must be confirmed
  resolved **before** any 051 parity gate is trusted.
- Estr/SFS stays on the 048 radix path (marginal cost measured 0.028 s/eval); the seam's fallback
  list encodes this — verify during audit.

## Stage 0 — Audit & reconciliation (local, no cluster time)

1. **Resolve the 2.6e-4 watch item**: confirm `baf8fb3` is in the current branch lineage and that
   the fixed kernel is what `fm049_rotor_verify.jl` (rewritten) exercises; if unresolved, add a
   small CPU-vs-CPU/GPU cross-check arm to the first cluster job before any parity gate.
2. **Audit host pair math** against references (line-by-line transcription check):
   - points functor vs FLOWVPM `src/FLOWVPM_fmm.jl:144–218` + `g_dgdr_gauserf`
     (FLOWVPM_kernel.jl:54–57), incl. the `r2==0`-only exclusion semantics (no `eps2` guard);
   - panel functor vs FLOWPanel `src/FLOWPanel_elements_fmm.jl:236–330` (`induced`,
     `rotate_to_panel`, `_is_self_pair` limits, `_induced_wake`, filament-regularization
     families baked as compile-time `Val` — never a runtime `FILAMENT_REGULARIZATION[]` read
     on device).
3. **Audit blind CUDA kernels** (`translate_batched_cuda.jl:8955–9126`): tiling/shared-mem
   correctness, one-thread-per-target no-atomics invariant, F64 vs F32 `rsqrt` paths,
   bit-identical summation order vs host (`_rect_point_pair` shared).
4. **Audit the FLOWPanel seam** (`FLOWPanel_gpu_influence.jl`): pass-recognition predicates vs
   `_sa_wake_influence!` (FLOWPanel_simulate.jl:713–812) and `_sa_body_influence!` (:813–874);
   fallback completeness; the documented kerneloffset deviation (all self-body pairs at
   `kerneloffset_panel` vs CPU's nearfield-only conditioning) — decide whether the 1e-3 parity
   gate absorbs it or the harness must compare like-for-like.
5. **Triage uncommitted FLOWPanel src mods**: `git diff` each; classify 051-load-bearing vs
   unrelated (paper-2/3 work); record verdicts. Do not commit anything yet.
6. **Run host tests** (julia-test-runner, ≤4 threads):
   `test/direct_rectangular_test.jl`; FLOWPanel full suite (`test/runtests.jl`) as the
   no-regression baseline snapshot; FastMultipole standard suite.
7. Record per-component verdict (adopt / fix / rewrite) in the 051 doc's worklog section.

**Exit:** every component either verified-by-audit+host-test or rewritten; 2.6e-4 item dispositioned.

## Stage 1 — points→targets kernel verified on hardware (051 stage 1)

- Add **CuMatrix testsets** to `test/direct_rectangular_test.jl` (host-vs-device parity on random
  shapes, F64 + F32, gradient on/off).
- Cluster **Job A** — `fm051_rect_bench.jl` pass 1 (210k real p018 particles → 36,752 targets):
  F64 device-vs-host parity ≤1e-11 on 2000 sampled targets; throughput vs the 0.02–0.04 s
  wake→panels estimate. Sync audited code to `~/FastMultipole-046`/`~/FLOWVPM-046` first
  (rsync of audited files only; never unaudited seams).
- Command for user (adjust job name):
  `ssh orc 'cd ~/FLOWVPM-046 && sbatch scripts/fm051_submit.sh'` (after auditing that submit
  script pins the fm048env module/env and H200 QoS).

## Stage 2 — panels→targets kernel + coupled-pass wiring (051 stages 1–2)

- Same Job A covers bench pass 2 (36,752 panels → 210k particle positions,
  `kerneloffset=1e-3`): parity + throughput vs the 0.4–2.0 s estimate (5–20× panel multiplier).
- **Coupled harness** (new FLOWVPM/FLOWPanel script, modeled on the 018 driver): full step head
  (`maneuver!` at FLOWPanel_simulate.jl:1185, `update_TE!` at :676–680) **before** any influence
  eval; NaN guard asserting the gotcha is exercised; then pass-by-pass comparison at the 018
  operating point — pass 1 (wake→bodies+probes+particles), pass 3 (bodies→probes+particles), and
  the SFS pass, each independently CPU-path vs seam-path, gate 1e-3 (F64 tighter where the
  kerneloffset deviation doesn't apply).
- Cluster **Job B** runs the coupled harness with seam `:cuda`, production D14+D15 untouched.
- No-regression gate: FLOWPanel CPU suite re-run locally with seam **unarmed** — identical to the
  Stage-0 baseline.

## Stage 3 — solve measurement, then (maybe) device matvec

- Measure at production shape (can piggyback Job B or be **Job C**):
  - Krylov iteration count: `solver.niter` capture at FLOWPanel `src/FLOWPanel_solver.jl:560`
    (KrylovSolver) or the FGS callback hook (`_solve!` :1036–1090, `FastMultipole.solve!`
    :1064–1075) for FGSSolver;
  - per-matvec cost of the `NearfieldInfluenceCache` path with `cache_tree`/`cache_nearfield`/
    `persistent_plan` on (solver docstring :360–390).
- Decision rule (from 050): iterations × matvec_cost vs 0.89 s headroom. Device-resident dense
  matvec (10.8 GB on H200, ~3.6 ms est.) implemented **only** if CPU solve doesn't fit; else
  CPU 64-thread solve stands and the device matvec is recorded as 052+ option.

## Stage 4 — timing report + closure

- From Job B/C timers: per-pass wall times against the B′ stack (0.373 + 0.04 + panels→particles
  measured + solve measured) vs 3.3 s/step; report as a table in the 051 doc Results.
- Docs: update `051-impl-panel-particle-gpu-coupling.md` (results, gate evidence with job IDs +
  artifact sha256s per 050 convention), handoff-phaseP update, START_HERE pointer. Commit audited
  code (FastMultipole fixes; FLOWPanel seam file + load-bearing mods) — **user approves commits**.
- Offer the pending lab-notebook entry (049/050 closures + 051 results) via notebook-drafter —
  user approval required before writing.

## Open items to resolve during execution

- **FLOWPanel cluster checkout**: handoff names only `~/FLOWVPM-046` and `~/FastMultipole-046`;
  the coupled harness needs FLOWPanel on orc — locate (check FLOWVPM-046 Manifest dev paths) or
  create, and record it in the handoff.
- Whether `fm051_rect_bench.jl`'s synthetic annular panel mesh is acceptable for Stage-1/2 kernel
  gates (it is; the *coupled* gates use the real 018 mesh) — keep the distinction explicit in the
  doc.
- Branch naming: FastMultipole is on `flowpanel-20260817`; FLOWPanel is on `fastmultipole`
  (HEAD `d6bf8b6`) — the docs' "flowpanel-20260817" for FLOWPanel is a misnomer; note in handoff.

## Verification summary

| Gate | How |
|---|---|
| Host kernel correctness | audit vs FLOWVPM/FLOWPanel references + `direct_rectangular_test.jl` (julia-test-runner, ≤4 threads) |
| Device kernel parity | Job A: fm051_rect_bench both passes, F64 ≤1e-11 on 2000 samples; new CuMatrix testsets |
| Coupled pass parity | Job B: pass-by-pass (1, 3, SFS) CPU vs seam at 018 point, ≤1e-3 phase gate, full step head + NaN guard |
| No regression | FLOWPanel full suite (seam off) identical to Stage-0 baseline; FastMultipole + FLOWVPM suites green |
| Solve pricing | Job B/C: niter × matvec cost vs 0.89 s headroom |
| Budget | Stage-4 table vs 3.3 s/step, job IDs + sha256 evidence recorded |
