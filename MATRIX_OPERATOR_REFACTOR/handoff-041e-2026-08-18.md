# Handoff: 041e in progress (2026-08-18)

Session state for the next agent. Read this + `START_HERE.md` + the 041e task
file; do NOT re-derive anything below.

## Completed this session (index already ticked, do not revisit)

- `041c` APPROVED (after regenerating its stale committed
  `data/multilevel_nearfield_shells/` by re-running the committed census
  script unmodified; a fresh reviewer then approved).
- `041d` APPROVED.
- `041f` DONE + APPROVED. Verdicts: NO-GO (sigma-adaptive smooth nearfield
  closed; rotor count-gate fail ~120x), unified-solver = one mesh family
  (037d global VIC), banded hybrid rejected by strict additivity. Artifacts:
  `theory/sigma-adaptive-multilevel-nearfield.md`,
  `scripts/sigma_adaptive_nearfield_census.jl`,
  `data/sigma_adaptive_nearfield/`.

## 041e status

- **Stage A: CONTINUE** — recorded in the task file (`Stage A record`
  section): rotor n=1e6 optimistic de-double-counted ceiling 32.7%
  (c1 underfill 36.1% dominates). Artifacts:
  `scripts/fm041e_target_owned_censusA.jl`,
  `data/target_owned_nearfield/censusA_*`.
- **Stage B implemented** (production code, all parse + load clean):
  - `src/containers.jl`: `DeviceAdaptiveCUDAContext` trailing fields
    `u_csr_offsets`, `u_csr_sources`, `u_csr_body_leaf`,
    `u_csr_built_epoch` — armed (sized) only when
    `FastMultipole.CUDA_NEARFIELD_SHAPE[] !== :pairs` at cache construction.
  - `src/tree_batched_cuda.jl`: `_cuda_adaptive_build_u_csr!` (deterministic
    target-major U CSR via (target-slot, index) key sort reusing V-partition
    scratch; boundary-fill offsets kernel; body->leaf map fill) called at the
    end of `_cuda_refresh_adaptive_lists!` (occupancy epochs).
  - `src/translate_batched_cuda.jl`: `CUDA_NEARFIELD_SHAPE`
    (`:pairs` default) + `NEARFIELD_SHAPES = (:pairs, :fused_cta,
    :fused_srclanes, :fused_packed)`; kernels
    `_cuda_direct_pairs_fused_cta_kernel!` (shape 1),
    `_cuda_direct_pairs_fused_srclanes_kernel!` (shape 4, warp-shuffle
    reduce), `_cuda_direct_pairs_fused_packed_kernel!` (shape 2, thread per
    body over leaf-major order = zero underfill);
    `_launch_cuda_fused_nearfield!` barrier; dispatch at the top of
    `_launch_cuda_nearfield_kernel!` (adaptive caches only, silent shipped
    fallback, `:lut` refused, graph/construction-baked Ref).
  - `test/cuda_radix_fused_nearfield_test.jl` (parity, CSR permutation,
    fallback, counters, P=4/8, F32/F64).
  - `MATRIX_OPERATOR_REFACTOR/scripts/fm041e_target_owned_bench.jl`
    (pre-registered Stage B driver; rotor loads
    `data/rotor_wake/rotor_snapshot_n{100000,1000000}.bin` — pre-extracted
    binaries so the GPU job needs no FLOWVPM; do NOT commit the .bin files).
  - `MATRIX_OPERATOR_REFACTOR/scripts/fm041e_submit_gpu.sh` (sbatch script).
- **H200 round 1 (job 13193465, complete)**: 292/292 tests pass, counters
  flat, accuracy identical across shapes. Bench
  (`data/target_owned_nearfield/stageB_bench.csv`, fetched locally): shapes
  1/4 FAIL the >=10% Stage B bar — best is `fused_cta` rotor 1e6 F32
  2.489 vs 2.678 ms = -7.1%; rotor 1e5 and F64 are worse than shipped;
  `fused_srclanes` loses everywhere material. NOTE: on the adaptive path the
  rotor nearfield is only ~10% of lifecycle (2.7/26.8 ms) — the 92–96% share
  quoted in the task file is the UNIFORM partitioned config.
  `sigma_multiscale` n=1e6 fails for ALL shapes incl. `:pairs` with
  "adaptive device DTR frontier capacity exceeded" — a bench-case
  (log-uniform sigma proxy) capacity defect, not a fused-path bug; rotor
  covers the sigma-heterogeneous 1e6 slot.
- **Round 2 (13193488)**: 386/387 — trivial defaults-assertion failure
  (tuple missing `:fused_packed`), fixed; no bench rows produced.
- **Round 3 (job 13193492) COMPLETE, results fetched locally**
  (`stageB_bench.csv` rows with job=13193492): **387/387 tests pass**, and
  `:fused_packed` (shape 2, the underfill-repacking thread-per-body kernel)
  **clears the >=10% Stage B bar** on the material cases at n=1e6:
  - rotor 1e6: near 2.019 vs 2.650 ms F32 (**-23.8%**), 3.221 vs 4.193 F64
    (**-23.2%**); lifecycle only -2.0%/-2.6% (adaptive nearfield share ~10%,
    so the 5% complete-solve promotion bar FAILS on the rotor);
  - cube 1e6: near -26.6% F32 / **-47.3% F64**; lifecycle **-10.9% F32 /
    -27.2% F64** — passes BOTH promotion bars outright;
  - wake 1e6 F64: near -9.8%, lifecycle -8.4%; wake F32 neutral;
  - ALL n=1e5 rows regress (worst wake F32 near +54%) — the >3%-regression
    gate forces a regime selector (fall back to `:pairs` at small n / small
    leaf counts).
  `sigma_multiscale` n=1e6 still fails for every shape incl. `:pairs`
  (bench-case DTR frontier capacity, not a kernel defect).

## Cluster flow (BYU rc, ssh alias `orc`)

- Snapshot: `orc:~/FastMultipole-041e` (rsync of this working tree,
  `--exclude .git`); env `orc:~/fm041eenv` (copy of fm041env with
  FastMultipole dev'd at the -041e path).
- Resync + submit: `rsync -a --exclude .git ./ orc:FastMultipole-041e/ &&
  ssh orc "source /etc/profile; sbatch ~/fm041e_gpu.sh"`
  (`~/fm041e_gpu.sh` is a copy of the submit script; re-copy it if the
  script changes). Always `source /etc/profile` before sbatch/squeue.
  Fetch results:
  `rsync -a orc:FastMultipole-041e/MATRIX_OPERATOR_REFACTOR/data/target_owned_nearfield/ MATRIX_OPERATOR_REFACTOR/data/target_owned_nearfield/`.
- The user's other slurm jobs (p1-*, fp-018-*) are unrelated — never touch.

## Next steps (in order)

1. Stage B verdict is in: retain ONLY `:fused_packed` for Stage C (shapes
   `:fused_cta`/`:fused_srclanes` are killed by the Stage B rule — record
   that, keep the code as measured evidence). Proceed to Stage C per the
   task file: full resident A/B matrix (both precisions, P=4/8 contract
   rows, adaptive winners + best-supported uniform geometry — note fused
   shapes engage only on adaptive caches, so the uniform rows are
   fallback-parity rows), gain attribution, and the regime selector
   (n/leaf-count envelope: adopt at large n, fall back at n=1e5).
   The likely overall outcome is **REGIME-ONLY**: cube 1e6 passes both
   promotion bars, the primary rotor case passes only the nearfield bar
   (complete-solve -2..-2.6% < 5% because the adaptive nearfield share is
   ~10%), and n=1e5 regresses everywhere.
2. Consider (cheap, optional) whether a `sigma_multiscale` bench-case fix
   (raise `AdaptiveTreePolicy` v/frontier capacity override in the driver)
   is worth one more H200 run to fill the sigma-heterogeneous 1e6 slot; the
   rotor already covers it if not.
3. Then: record the Result section (GO/REGIME-ONLY/NO-GO + attribution) in
   the task file, tick Done there + in START_HERE, spawn a clear-context
   approval subagent (approver ≠ completer). Keep `CUDA_NEARFIELD_SHAPE`
   default `:pairs`; any default change needs explicit user approval.
4. Housekeeping to raise with the user at close: nothing is committed this
   session (041c regen + 041f + 041e all uncommitted, per the
   no-commit-unless-asked rule); the two rotor `.bin` snapshots under
   `data/rotor_wake/` (5.6/56 MB) should be gitignored, not committed.
5. After 041e closes, the next index rows are `041g` then the `042`
   milestone review (all other 042 blockers are now Done+Approved).
