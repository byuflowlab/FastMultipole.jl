# Handoff: start 041h (2026-08-18)

Supersedes `handoff-041e-2026-08-18.md` (kept for the 041e detail record).
Read this + `START_HERE.md` + `041h-impl-real-rotor-simulation-fullstep.md`;
do not re-derive anything below.

## Session-closing state

- `041c`, `041d`, `041f` all DONE + APPROVED (see the older handoff and the
  task files for detail; 041f verdict NO-GO with the unified-solver and
  banded-hybrid amendments answered).
- `041g` was completed + approved by a PARALLEL session (NO-GO), not this
  one — its index row and artifacts are not this session's work.
- `041e` DONE, verdict **REGIME-ONLY** (Result section in the task file is
  the record): shape-2 `:fused_packed` ships off-by-default behind
  `FastMultipole.CUDA_NEARFIELD_SHAPE` (construction/graph-baked) with the
  `CUDA_NEARFIELD_FUSED_MIN_BODIES = 400_000` regime selector; shapes
  `:fused_cta`/`:fused_srclanes` measured-killed. Headlines: cube 1e6
  lifecycle −10.9% (F32) / −27.2% (F64) — both promotion bars pass; rotor
  1e6 nearfield −24% but complete-solve only ~−2% (adaptive-path nearfield
  share is ~10%, NOT the uniform-config 92–96% the task file assumed); all
  n=1e5 rows regress → selector zeroes them. H200 evidence: jobs
  13193465/13193488/13193492/13193508, tests 387/387, data in
  `data/target_owned_nearfield/stageB_bench.csv`. Clear-context approval
  status: see the note at the end of the 041e task file and the Approved
  checkbox in START_HERE (finalized at session close).

## Next task: 041h (user-directed, staged, first unblocked row)

`041h-impl-real-rotor-simulation-fullstep.md` — full per-step optimization
on the REAL FLOWPanel rotor_hover wake fields, single H200, plus a modeled
8-GPU estimate. Key facts already scouted:

- Sources (do not re-explore): primary
  `../../FLOWPanel.jl/data/rotor_hover/rotor_hover/rotor_hover_wake_particles/rotor_hover_wake_particles.719.vtp`
  (final of 720 steps, n = 67,745); secondary
  `../../FLOWPanel.jl/data/rotor_hover_relax006_full/rotor_hover_relax006_full_wake1_particles/rotor_hover_relax006_full_wake1_particles.921.vtp`
  (final of 922, n = 37,165). VTP = VTK PolyData, zlib appended encoding,
  per-particle arrays: Points(3), gamma(3), sigma(1), vol, circulation,
  velocity(3), vorticity(3), C(3), SFS(3), velocity_gradient(9). Mid-run
  steps of the primary series exist every step (0..719) for the real
  refresh/epoch-persistence measurement.
- The task file's "Regime honesty" section lists the expected shape of the
  answer (latency/refresh-floor-dominated; fused kernel below its envelope;
  uniform ell=2/3 competitive at small n; 028/029 ~0.87 ms per-GPU floor).
- Execution order per the task file: (1) extraction script + committed
  snapshots + census + sampled-direct references; (2) pre-registered
  uniform-vs-adaptive baseline matrix on H200; (3) profiled optimization
  cycles, each production change user-gated; (4) 8-GPU model with break-even
  n (no multi-GPU implementation).

## Cluster flow (unchanged from the 041e handoff)

ssh alias `orc`; snapshot `~/FastMultipole-041e` (rsync
`rsync -a --exclude .git ./ orc:FastMultipole-041e/`), env `~/fm041eenv`
(FastMultipole dev'd at that path). Submit pattern: put an sbatch script in
`MATRIX_OPERATOR_REFACTOR/scripts/`, copy to `~` on the cluster, `ssh orc
"source /etc/profile; sbatch ~/<script>.sh"` (always source /etc/profile);
outputs land in `orc:~/<jobname>_%j.out`; fetch data dirs back by rsync.
H200 partition via `--gpus=h200:1` (see
`scripts/fm041e_submit_gpu.sh` as the template). The user's other slurm
jobs (p1-*, fp-018-*) are unrelated — never touch. For a NEW task snapshot
dir, either keep using `-041e` (it contains the current tree) or make
`-041h` + env the same way (`cp -r fm041env fm041henv` + Pkg.develop).

## Housekeeping owed to the user

- NOTHING from 2026-08-18 is committed (041c data regen, 041f, 041e code +
  data, 041h staging, the two handoff files). The user asked for a
  proposed commit structure before anything lands — propose, don't run.
- `data/rotor_wake/rotor_snapshot_n*.bin` (3 files incl. n=316228) are
  gitignored on purpose (synthetic-rotor extracts for cluster jobs).
- The 041h VTP-extracted snapshots, when created, SHOULD be committed
  (small, real-data provenance) per the task file.
