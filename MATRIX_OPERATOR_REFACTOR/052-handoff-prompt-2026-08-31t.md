# 052 handoff (session 2026-08-31t): reverse leg BUILT+GPU-VALIDATED; gpu40 landed; LG twin fixed and running

## Prompt for the next agent

You are continuing task 052 after a context reset. Session 2026-08-31t
implemented the ENTIRE 052h reverse leg (particles→panels FMM, stages
R0–R5) and GPU-validated it (R6), landed the gpu40 production run,
root-caused and fixed the LG twins' recurring Krylov death, and fixed a
real 052f device bug plus a stale test. **Your first actions, in order:**

1. **Re-arm the monitors** (session-local, died at reset; recipes below).
   Check job states immediately.
2. **Babysit the LG run 13518861 and cpu40-r3 13518479** (details below).
   The LG twin-cancel is DONE (13518862 canceled) — 13518861 is THE run.
3. **Next work item: the reverse-leg s038v spot-check plan** (below), and
   anything Ryan asks. There is a LOT of uncommitted work — see "Dirty
   trees" — do NOT commit or push without Ryan's ask.

## Cluster state (as of 2026-08-31 ~04:0x MDT)

ssh recipe: `ssh orc 'bash -lc "source /etc/profile; ..."'`, user
rander39; memory `orc-cluster-access.md` (banner glues to first stdout
line — lead with a bare `echo`; combined multi-job squeue/sacct queries
drop pending jobs — one id at a time; `~/st052_probe.sh` does this, ids
now `13518861 13518862 13518479 13518480` — EDIT to drop 13518862/480).
Nested-quoting through ssh is fragile: for anything beyond one command,
write a script locally, `scp` it to orc, run `bash ~/script.sh` (this
session's pattern: k_check.jl, fix178.sh, stitch40.sh, arch40.sh).

- **13518861 `fp-il-s038v-gpu40lg`** (052 silo `~/FLOWPanel-052-h200`,
  m13h h200): RUNNING, LineGauss production run, FRESH start (not a warm
  restart), at ~step 672/1475 and stepping cleanly, 0 errors. Twin
  13518862 CANCELED (singleton twin-cancel done). **LineGauss perf check
  DONE:** early steady steps ~3.8–3.9 s/step vs gaussian-era baseline
  ~4.9–5.3 → no regression (~20% faster at face value; confound: 052
  stack ≠ 018 stack). Log:
  `~/FLOWPanel-052-h200/logs/slurm/slurm-fp-il-s038v-gpu40lg-13518861.{out,err}`.
  When it lands: same landing pattern as gpu40 below (harvest, note,
  judge by outputs not sacct — the wrapper NaN gate trips on the
  `CT KJ = NaN` diagnostic column whenever RUN_KJ is off).
- **13518479 `fp-il-s038v-cpu40-r3`** (m12, `~/projects/FLOWPanel.jl`):
  RUNNING, was ~step 443/1475 at ~01:45, ~150-170 s/step. Host-only.
- **13518480 gpu40: LANDED.** Ran 1475/1475 (416 steps post-restart at
  ~26 s/step, 10,953 s). **052g validated in production.** sacct FAILED
  = wrapper NaN gate on the expected `CT KJ = NaN` table; run printed
  `all finite = true`. Readout: cycle-mean CT 218.171 ± 4.27; Phase-2e
  CONVERGED = false (within-rev p-p/mean 0.921 vs tol 0.02) — flag to
  Ryan. Monitors stitched (0–1059 crashed + 1060–1475 restart) into
  `~/FLOWPanel-018-gpu-gh200/data/scr_p019_s038v_gpu40/monitors_stitched/`
  (1476 steps each; originals untouched). Crashed dir archived (2.2 MB
  `...crashed1061.archive.tar.gz`) and renamed
  `scr_p019_s038v_gpu40.crashed1061.todelete` (25 G; pre-1060 VTK series
  is INSIDE it). Landing note appended to
  `FLOWPanel.jl/plans/sigma_vpm_illustrations_20260827/HANDOFF.md`.
- **13518724 `fm052h-validate`: COMPLETED, all stages PASS** (see 052h).

## LG twin root cause (SOLVED — do not re-fight this)

The twins' repeated 3-second Krylov deaths (13512551, 13512552,
13518478, 13518730, 13518731) were NEVER a depot problem. The twin
dispatcher (`~/FLOWPanel-052-h200/examples/run_p018_screen_hpc.slurm.sh`
line 178) ran `julia --project=.` in the silo, which has NO
Manifest.toml (untracked, never synced). The 31s "fix" instantiated the
wrong project (`~/fm052env-h200`, which was already fine). Copying
`~/projects/FLOWPanel.jl/Manifest.toml` also fails — it was generated by
Julia 1.12 (JuliaSyntaxHighlighting stdlib) vs the jobs' 1.11.7.
**Durable fix applied:** dispatcher line 178 is now
`julia --project="${P018_PROJECT:-.}"` → `~/fm052env-h200` (which
path-devs the silo stack; `import FLOWPanel` verified on login node).
NOTE this edit lives ONLY in the h200 silo dispatcher; if other silos'
dispatchers share the `--project=.` pattern they have the same landmine
(the gh200 018 silo has its own depot/dispatcher and worked — don't
touch it).

## 052h reverse leg: COMPLETE through GPU validation

Full status header in `052h-reverse-leg-subplan-2026-08-31.md` (read
it). Summary — all built THIS session, all validated:

- **R0** LH dual-channel operator tables (`cross_stencil_host.jl`:
  `cross_m2m/m2l/l2l_operators_lh`, `cross_m2l_level_scales_lh`).
  Covariance law SOLVED: diagonal blocks scale `n+n'+1`, φ←χ mixing
  `n+n'` (one power weaker), χ←φ ≡ 0; still separable (χ rows n+1,
  χ cols n−1). 554 tests in `test/cross_stencil_test.jl`, green.
- **R1** reverse producers (`cross_stencil_cuda.jl`): `build_reverse`
  kwarg on `device_cross_producer_context` (panel-side dense
  `d_node_at_panels`, `d_rev_route_*`/`d_rev_block_*` lists,
  `n_rev_routes/n_rev_demoted/n_rev_blocks`), `_cross_generate_level!`
  parameterized on (src occupancy, dense node_at), reverse lists in
  `download_cross_lists`.
- **R2/R3** `DeviceCrossReverseState` + `device_cross_reverse_state` +
  `refresh_cross_reverse_multipoles!` (gathered sorted 7-row buffer →
  radix `_cuda_b2m_vortex_leaf_nodes_kernel!` reused bit-for-bit via
  strided φ/χ views of stacked storage; LH M2M via the D-agnostic
  `_cross_m2m_kernel!` at 2D) + `refresh_cross_reverse_locals!`
  (`_cross_m2l_lh_kernel!`, row/col scale tables) +
  `finish_cross_reverse_locals!` (LH L2L; `_cross_l2b_kernel!`
  generalized to separate phi/chi args — forward call site updated).
- **R4** `_cross_near_vortex_kernel!` + `apply_cross_reverse_near!`
  (gaussianerf `REG`, `sigma_row` arg, 052g zero-work early-outs).
- **R5** FLOWPanel wiring (`FLOWPanel_gpu_influence.jl`, local repo
  branch `fastmultipole`): gates `PANEL_WAKE_FMM` (default off) /
  `PANEL_WAKE_FMM_DEVICE` (default on) / `PANEL_WAKE_FMM_XVERIFY`;
  `_ReverseWakeEntry` + `_REVERSE_STATE` cache (frozen padded box,
  grow-only `_REV_BUF` 8-row particle buffer — padding = wrapped real
  positions + ZERO strength + σ=1; rebuild-on-escape;
  fallback-before-accumulate); `_panel_wake_reverse_device!`; hook in
  `_gpu_rect_influence!` pass1 (`fmm_wakes`, dense-pack skip, the
  `isempty(...)&&continue` guard includes fmm_wakes). v1 = BODY targets
  only; probes stay dense (probe-object identity across steps
  unresolved — a per-step entry rebuild would swamp the win). Requires
  CuArray-backed pfield (host-backed falls back dense). Loads clean.
- **R6 GPU validation: job 13518724 ALL PASS** — stage 1
  `cuda_radix_interface_test.jl` 1241 pass (incl. the 052f/052g stage-7
  testsets); stage 2 p34 36/36 (forward AND reverse lists bit-exact vs
  host oracle, step-472 snapshot `~/snapshot472-052d`, 3 configs);
  stage 3 p53 8/8 (device far vs host oracle dev 1.7e-15, near 2.6e-16,
  total at exactly the host P=8 expansion error 1.306e-02).
- **Host oracle:** `prototypes/052d_cross_stencil/p52_lib.jl` (fixture +
  host pipeline) + `p52_reverse_host_oracle.jl` (6/6: P-convergence
  max relU 3.0e-1→1.1e-3 over P=4→12, mean 2.1e-5) +
  `p53_reverse_device_replay.jl` (GPU). p34 extended with reverse
  checks. GPU infra: silo `~/FastMultipole-052h` + env `~/fm052henv`
  (clone of fm052env-h200 with FastMultipole repointed) +
  `~/fm052h_validate.sh` (3-stage sbatch; submit plugin REJECTS explicit
  `--partition` lines — omit them).

## Bonus fixes this session (in the dirty trees)

1. **Real 052f device bug** (caught by the stage-7 test):
  `_alldirect_geometry_fallback!` (`translate_batched_resident.jl`
  ~:2160) ran during DEVICE construction before `cache.state` exists →
  `nothing.sfs` crash. Fixed: sfs config read from
  `device_ctx.sfs_ctx` when state is nothing.
2. **Stale test**: `test/hierarchical_m2l_host_test.jl:409` expected
  `RigidHierarchicalTables(21)` to throw, but 052f intentionally widened
  the supported radii to include 21. Now uses 23 (not a sum of three
  squares — permanently unsupported). Full local suite green after fix.

## Dirty trees (NOTHING committed this session — Ryan's call)

- FastMultipole `flowpanel-20260817`: cross_stencil_host.jl (+192 LH),
  cross_stencil_cuda.jl (+477 reverse leg), translate_batched_resident.jl
  (sfs fix), test/cross_stencil_test.jl (+140),
  test/hierarchical_m2l_host_test.jl (radius fix), p34 extension,
  NEW: p52_lib.jl, p52_reverse_host_oracle.jl, p53_reverse_device_replay.jl,
  052h-reverse-leg-subplan-2026-08-31.md, this handoff.
- FLOWPanel.jl `fastmultipole` (local): FLOWPanel_gpu_influence.jl (R5
  wiring), plans/.../HANDOFF.md (gpu40 landing note, append-only).
- orc silos: `~/FastMultipole-052h` mirrors the local dirty tree;
  `~/FLOWPanel-052-h200` dispatcher line-178 fix (production-relevant!).

## Monitors to re-arm (notify-only; scripts survive at
`<scratchpad>/mon/` but the scratchpad is session-specific — recreate)

1. **Job states** loop ~300 s: `bash ~/st052_probe.sh` via ssh, grep
   `ST8:`, diff vs prev, notify on change. Edit probe ids first (drop
   13518862 canceled + 13518480 landed).
2. **LG 13518861 log**: loop ~300 s, grep
   `resuming|Falling back|GATE|LoadError|ERROR|CUDA error|Killed|OOM|launcher done`
   over its out/err; new-lines-only; on ERROR read the tail and diagnose
   before acting. Monitors must be notify-only (no embedded scancel/rm —
   classifier blocks them; learned repeatedly).
3. NOTE: legacy Monitors from older sessions may still emit noisy
   `<task-notification>` events with stale job ids — ignore them.

## NEXT: reverse-leg s038v spot-check (needs Ryan's timing call)

Goal: end-to-end `PANEL_WAKE_FMM=1 PANEL_WAKE_FMM_XVERIFY=1` short run
(5e-style, NREVS=0.05) to get relU + step-time delta for the reverse
leg in production geometry. Needs a FLOWPanel+FLOWVPM+FastMultipole silo
carrying this session's dirty trees — do NOT touch `~/FLOWPanel-052-h200`
/ `~/projects/*` while 13518861 / 13518479 run from them. Recipe: fresh
silo trio (rsync local trees), clone fm052env with all three repointed,
adapt the step6 xverify script pattern
(`~/FLOWPanel-052-h200/fp052d_step6_linegauss.sh`). Bundle with any
other pending GPU work. After validation: perf numbers → Ryan decides
default-on. Also future: probes leg (needs probe identity), U+J hessian
channel, `build_forward=false` producer option (reverse-only entries
currently pay a wasted forward sweep).

## House rules (carried forward)

4 threads max locally; julia-test-runner for all runs/scripts (raw
output → scratchpad log, grep it); refactor-docs-librarian for
MATRIX_OPERATOR_REFACTOR doc questions; verifier before reporting
claimed numbers; never read `data/**`/`*.csv`/`*.bin` directly. GPU
jobs authorized; combine stages into one sbatch; h200 submit needs NO
--partition line. Notebook writes need Ryan's approval FIRST (he said
not yet). Commits/pushes only on Ryan's ask. Auth expiry → ask Ryan to
run `! ssh orc echo ok`. rsync `--checksum`.

## Open Ryan decisions (ask, don't assume)

- Commit this session's work? (trees dirty, itemized above)
- Push to remotes? (still undiscussed)
- Notebook entries (deferred; when he asks, offer topic list: 052h
  reverse leg + covariance law, gpu40 landing + CT non-convergence,
  LG twin root cause, 052f sfs bug)
- Final `rm -rf` of THREE `.todelete`/archive dirs: leakprobe (018 silo),
  `scr_p019_s038v_gpu40.crashed1061.todelete` (25 G, contains pre-1060
  VTK), and whether to keep `~/FastMultipole-052h`+`~/fm052henv` after
  052h validation (small, suggest keep until reverse leg ships).
- gpu40 CT non-convergence (within-rev 0.921 vs 0.02): expected at this
  mesh/NT, or investigate?
- s038v reverse-leg spot-check timing (silo + GPU job).
- `.prev` archive disappearance (pre-30r): still unconfirmed it was him.
